const service=document.getElementById('service'), addons=document.getElementById('addons'), total=document.getElementById('total'), date=document.getElementById('date');
function calc(){let n=Number(service.selectedOptions[0].dataset.price);document.querySelectorAll('#addons input:checked').forEach(x=>n+=Number(x.value));total.textContent='$'+n.toLocaleString('en-CA')}
service?.addEventListener('change',calc);document.querySelectorAll('#addons input').forEach(x=>x.addEventListener('change',calc));
document.getElementById('check')?.addEventListener('click',async()=>{const box=document.getElementById('availability'); if(!date.value){alert('Please choose a date.');return} box.hidden=false; box.textContent='Checking availability…'; try{const r=await fetch('/api/availability?date='+encodeURIComponent(date.value)+'&service='+encodeURIComponent(service.value)); const d=await r.json(); if(d.available){box.innerHTML='<strong>Date looks available.</strong><br>Choose your add-ons below, then continue to secure your booking.';addons.hidden=false;calc()}else{box.innerHTML='<strong>This date is unavailable.</strong><br>Please choose another date or <a href="/contact.html">contact us</a> for a custom option.';addons.hidden=true}}catch(e){box.innerHTML='<strong>Availability check is not connected yet.</strong><br>Please use the contact page while Square credentials are being configured.'}});
document.getElementById('checkout')?.addEventListener('click',async()=>{const items=[{name:service.selectedOptions[0].text,price:Number(service.selectedOptions[0].dataset.price)}];document.querySelectorAll('#addons input:checked').forEach(x=>items.push({name:x.dataset.name,price:Number(x.value)}));const r=await fetch('/api/checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({date:date.value,service:service.value,items})});const d=await r.json();if(d.url)location.href=d.url;else alert(d.error||'Checkout is not configured yet.')});